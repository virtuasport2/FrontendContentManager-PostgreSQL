window.__CONFIG__ = {
  // REMOTO
  API_BASE_URL: "https://api-content-manager-postgresql.onrender.com",
  WS_URL:"wss://api-content-manager-postgresql.onrender.com/logs"
  // Anche se sembrano uguali:
  // localhost e
  // 127.0.0.1
  // per il browser possono comportarsi in modo diverso (soprattutto con CORS, sessioni, cache e cookie).
  // API_BASE_URL: "http://localhost:8080",
  // WS_URL: "ws://localhost:8080/logs"
};
